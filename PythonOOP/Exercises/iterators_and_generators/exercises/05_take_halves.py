def solution():

    def integers():
        max_level = float('inf')
        integers = (i for i in range(int(max_level)))


    def halves():
        halves = []
        for i in integers:
            halves.append(i / 2)

    def take(n, seq):
        # TODO: Implement

    return (take, halves, integers)
