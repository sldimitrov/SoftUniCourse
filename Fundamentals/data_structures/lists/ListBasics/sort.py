# Define a function
def sort_function(unsorted_list: list):
    for element in input_list:
        list_with_integers.append(int(element))
    return sorted(list_with_integers)

# Read User input
input_list = input().split()
list_with_integers = []
list_with_integers = sort_function(input_list)
print(list_with_integers)