my_list = [1, 2, 3, 4, 10]

# Taking the last element by default
popped_element = my_list.pop()
print(popped_element)

# Popping by index
popped_element_by_indices = my_list.pop(0)
print(popped_element_by_indices)