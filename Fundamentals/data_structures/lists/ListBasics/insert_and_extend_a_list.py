my_list = [1, 2, 3]

# First parameter is the index
# The second is the number we insert at that exact index
my_list.insert(3, 4)

# We extend our list with these integers
my_list.extend([5, 6, 7])

# Another way to extend a list:
my_list += [8, 9, 10]

print(my_list)