from core import divide, multiply, subtract, addition, raising

# Initialise a mapper dictionary
sign_mapper = {
    "/": divide,
    "*": multiply,
    "-": subtract,
    "+": addition,
    "^": raising,
}